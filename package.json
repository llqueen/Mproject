{
  "name": "njob-scanner",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "openai": "^4.54.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
